language: node_js
node_js: stable

install:
  - npm install

#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "newpromise"
  - git config user.email "250621458@qq.com"
  - git add .
  - git pull "https://${GL_TOKEN}@${GH_REF}" master:master
  - git push "https://${GL_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - origin
env:
 global:
   - GH_REF: github.com/newPromise/newPromise.github.io.git